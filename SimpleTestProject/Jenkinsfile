pipeline {
  
  agent any
  
  stages{
    
    stage("build"){
      steps{
        echo 'building the application'
        echo 'making a change at 2:03 am'
      }
    }

    stage("test"){
      steps{
        echo 'testing the application'
        // On Linux/macOS use sh, on Windows use bat
        // sh 'mvn clean test'
      }
    }

    stage("deploy"){
      steps{
        echo 'deploying the application'
      }
    }
  
  }
}
